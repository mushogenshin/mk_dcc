from pathlib2 import Path

import pymel.core as pm

_FILE_TYPE = 'file'

all_texture_files = pm.ls(type=_FILE_TYPE)

all_texture_files[0].name()

for texture_file in all_texture_files:
    print(texture_file.fileTextureName.get())
    old_ftn = Path(texture_file.fileTextureName.get())
    if old_ftn.parent == old_root
    
    new_root / old_ftn.name
    
    
    
